package plain.contract.loop;

/**
 * It repeats something without parameter in the method.
 * @author Rin
 * @version 1.0.0
 */
public interface PlainRepeatable {
	/**
	 * Repeat something.
	 * @since 1.0.0
	 */
	void repeat();
}
