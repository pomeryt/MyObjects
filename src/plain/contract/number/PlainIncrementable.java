package plain.contract.number;

/**
 * Responsible for incrementing the number.
 * @author Rin
 * @version 2.0.0
 */
public interface PlainIncrementable {
	/**
	 * Increment the the number.
	 * @since 1.0.0
	 */
	void increment();
}
