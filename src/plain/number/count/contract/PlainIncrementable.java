package plain.number.count.contract;

/**
 * Responsible for incrementing the integer by 1.
 * @author Rin
 * @version 1.0.0
 */
public interface PlainIncrementable {
	/**
	 * Increment the integer by 1.
	 * @since 1.0.0
	 */
	void increment();
}
